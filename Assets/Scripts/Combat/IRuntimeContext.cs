namespace PirateRoguelike.Combat
{
    public interface IRuntimeContext
    {
        // Add properties here as needed for runtime data access in descriptions
        // Example: ShipState Caster { get; }
        // Example: ShipState Target { get; }
    }
}
